# 🧮 Calculator App (Flask + Docker)

A simple web-based calculator built using **Python Flask** and containerized with **Docker**.  
This project demonstrates how to build, containerize, and run a web application using Docker — a core DevOps skill.

---

## 🚀 Features

- Add, subtract, multiply, and divide numbers
- Simple web UI
- Built with Flask (Python)
- Fully Dockerized
- Easy to run anywhere using Docker

---

## 🛠 Tech Stack

- Python 3
- Flask
- Docker
- HTML

---

## 📦 How to Run (Without Docker)

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
